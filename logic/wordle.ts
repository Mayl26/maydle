// 1) verify the word is actually in valid words
// 2) check which letters are yellow and which are green

// check word is valid word in the list
function isValidWord(word: string): boolean {

}

// returns the indices of yellow letters and the indices of green letters
function checkLetters(word: string, answer: string): { yellow: number[], green: number[] } {
    
}